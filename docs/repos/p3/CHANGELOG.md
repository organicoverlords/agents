- [2026-09-07] [ci] Added a plan-first exact-head PR merge guard that enforces current contract/cohort evidence before P3 merges, preventing C++ heads from bypassing shared integration validation without adding a queue or scheduler (#296).
- [2026-09-07] [build] Aligned the repo-owned OMEN build facade with the current isolated Linux lane-2 queue, preserving exact committed snapshots and refusing unsupported direct-host build semantics (#2355).
- [2026-09-05] [ci] Clarified exact-head PR merge admission so native GitHub auto-merge cannot be mistaken for P3 contract/cohort success (#530).
- [2026-08-31] Bound the #456 eight-role weapon/projectile gap to Tiny3D PR #161 source-audit evidence, recording all roles as explicit LowVRAM SOURCE_ABSENT generation gaps instead of an unattributed generic qualification gap (#456).
- V2 Construction: add an authority-only repair transaction that restores live registered structure health, clamps at max health, and refuses destroyed/unregistered structures.
- [2026-08-31] Primary weapon and projectile presentation keeps diagnostic fallbacks, exposes Config/Game production bindings, and enforces an eight-role hash-qualified family contract so fallback shapes cannot be accepted as finished art (#456).

- [2026-09-03] Lane War creeps now reserve direct-to-core movement for worlds without navigation data, so live dynamic construction blockers can actually reroute AI pathing (#206).
- [2026-09-04] Flying jellyfish now turn to face the defense core before stopping to attack, preventing visibly off-target attacks on curved approaches.
- [2026-09-04] [meta] Completed the P3 canonical agent-rule cutover: all agent-facing policy/start/routing/procedure files are pointer-only, the retired local policy injector is removed, and validators now enforce pointers plus real implementation boundaries instead of copied prose.
- [2026-09-04] [meta] Retired the unused worker-liveness supervisor/repair registry; runner busy remains transport metadata, while active work comes from concrete process/runtime/output evidence.
- [2026-09-04] [meta] Retired 30 branch-pinned Aug-16/17 feature/integration proof, build, and diagnostic workflows whose source branches were deleted; current V2 proof/build tooling remains.
- [2026-09-04] [policy] Require live progress evidence before treating a P3 build resource as occupied; safely reap clearly abandoned non-editor build/query residue instead of queueing indefinitely.
- [2026-09-04] [product] Prioritized first-person and third-person V2 gameplay/acceptance; deferred RTS/overhead camera, input, UI and proof work until explicitly reactivated.
- [2026-09-04] [meta] Removed the autonomous P3 fleet lifecycle planner/sweeper; `P3_HANDOFF_*` remains passive PR-local evidence, BusyCoordinator remains exact collision ownership, and GitHub issues/PRs remain work state.
- [2026-09-04] [meta] Removed the synthetic infrastructure wall-clock probe from global PR contract admission; task-local LFS time bounding remains in its owning operation.
- [2026-09-04] Chose initial #803 base assets: existing hash-locked BaseFringe Core, V2 StiltHut as the first base/house promotion target, and visually approved WeatheredWoodWallBrace as the first defence wall.
- [2026-09-04] Made the first friend-playable V2 Lane War the immediate product priority: two defended movable-core bases linked by a creep road, integrated grapple/ski/earthworks/build/destruction/spells, animated characters, gold economy, and complete two-player proof (#611).
- [2026-09-04] Defined the V2 product thesis as one 3D Lane War battlefield combining skill combat, destructive tactics, momentum skiing, rapid construction, and live earthworks that reshape movement and creep routes (#611).
- [2026-09-04] [meta] P3 lane creation no longer recycles a different branch's clean worktree; only an already-checked-out requested branch is reused, while new branches allocate separately or respect real lane/editor caps.
- [2026-09-04] [meta] Retired the dead fleet PR-budget/admission API and backpressure decisions; the hourly lifecycle sweep now remains duplicate-PR maintenance with advisory handoff reporting only.
- [2026-09-04] [meta] Fixed the lane PlanOnly contract test so valid in-process PowerShell planner output is not rejected because a prior native git command left a stale `$LASTEXITCODE`.
- [2026-09-04] [meta] Removed the open-PR-count queue admission gate so independent P3 pull requests no longer fail or wait solely because more than eight PRs are open; exact duplicate retirement remains separate maintenance.
- [2026-09-03] P3 Tiny3D result writing now preserves explicit runtime attribution for missions that require visual-motion proof, without changing legacy static-result behavior (organicoverlords/tiny3d#15).
- [2026-09-03] Canonical RenderOffscreen visual proof now renders directly from the live PIE player camera through SceneCapture2D; the known-bad `capture_screenshot target=pie` viewport path is fail-closed.
- [2026-09-03] P3 Tiny3D ground-skeletal materialization now accepts the current v2 locomotion contract only when its walk-loop requirement and P3-owned in-place motion policy match the producer contract.
- [2026-09-02] Canonical PIE visual capture now uses the actual game viewport widget before SceneCapture fallback so HUD/UMG claims cannot silently lose UI (#771).
- [2026-09-02] Completed the final-asset LFS publisher with guarded branch-ref publication: before its process-local LFS pre-push skip, it proves the branch LFS delta exactly matches the reviewed object set and preserves the persistent emergency guard (#760).
- [2026-09-02] P3 #414 environment gaps now carry the current Tiny3D candidate asset/package identities for bridge, boundary landmark, and shelter roles without changing existing gameplay geometry or collision.
- [2026-09-02] Local visual proof review now consumes the worker report exact run path directly, stores canonical manifest.json/reviewed.json in that write-once run, and removes the global latest.json race and reviewed-media duplication (#768).
- [2026-09-02] [meta] Aligned P3 worker selection, ownership, delivery states, and multiplayer direction with Stack Delivery and the current V2 Lane War path (#767).
- [2026-09-02] Visual proof now uses write-once local `C:\P3Proofs\<run-id>` folders as the canonical artifact path; capture/review no longer depend on Google Drive or another cloud mount (#765).
- [2026-09-02] Visual proof publication now exposes PENDING_REVIEW and reviewed.json pointers so worker reports can link the actual local proof run while only independent review promotes the claim state (#762).
- [2026-09-02] Hardened V2 retirement guard coverage for legacy raw-key flight and Ice Mountain pawns so ordinary PRs cannot extend those V1 input authorities outside #610/#611.
- [2026-09-02] Added an explicit exact-object final-asset LFS publisher: plan-only by default, bounded and receipt-backed, with generated proof/media rejection and process-local push-guard override (#760).
- [2026-09-02] Retired two dead branch-pinned self-hosted proof workflows that could still trigger remote Git LFS downloads after their source branches disappeared (#753).
- [2026-09-02] Remote P3 LFS hydration is now opt-in by default: fresh clones do not fetch network LFS unless explicitly enabled by environment or repo config (#753).
- [2026-09-02] Worker Git LFS hydration is now local-cache-first and batched: cached objects never trigger remote fetches, quota-disabled repos fail fast, and editor/runtime paths no longer use broad git lfs pull (#753).
- [2026-09-02] V2 Meteor presentation now uses a feature-owned mesh sequence with readable descent, impact core, shock ring, debris, and settle timing instead of generic placeholder Niagara/flash presentation (#603).
- [2026-09-02] P3 lane reuse no longer treats BUSY issue-number matches or stale editor labels as resource-capacity truth; editor slots track hydrated/build/runtime occupancy, and new compile lanes force source-class metadata.
- [2026-09-02] Visual proof captures now carry the intended claim, and PROVEN requires an independent claim-matched pixel/clip review with an explicit frame/time window; pixel-content checks remain capture-health only.
- [2026-09-02] Lane War core clients now apply the exact config-bound production material alongside the core mesh without changing gameplay collision or authority (#413).
- [2026-09-02] Lane War core production binding now pins the exact core material asset and LFS SHA-256 alongside the existing mesh provenance contract (#413).
- [2026-09-02] V2 stump qualification gap now records its exact source reference and SHA-256 while preserving the existing NoCollision binding (#414).
- [2026-09-02] V2 environment qualification-gap receipts now preserve each role's live collision contract so visual asset swaps cannot silently change Lane War geometry semantics (#414).
- [2026-09-02] Storage recovery now sweeps nested P3 generated caches at a 75 GiB trigger toward a 100 GiB target before normal admission, while preserving active lanes and user/evidence data.
- [2026-09-02] Editor lane admission now reclaims only proven-cold build caches and rechecks disk before refusing promotion, preserving active and warm worker state.
- [2026-09-02] V2 TowerDefense authority now rejects non-finite enemy health, damage, and tower attack values before they can mutate replicated combat state (#607).
- [2026-09-01] Reset V2 combat runtime acceptance stage and cooldown state during shutdown so repeated sessions cannot inherit stale PASS/progression state (#603).
- [2026-09-02] Switched the project defaults to the V2 production world/GameMode and retired legacy p3 world helpers from V2 GameMode/GameState worlds (#599, #610).
- [2026-09-01] Clarified worker routing docs so timed workers cannot substitute or retry unrelated GitHub workflows when Actions reports `(Unknown event)` / `startup_failure`.
- [2026-09-01] Removed stale P3 CI/docs references to obsolete verification and custom UI source-build paths.
- V2 traversal: move the authoritative dash ability from P3Gameplay into the feature-owned P3Traversal plugin, preserving semantic input and server cooldown behavior. (#603)
- [2026-09-01] Fixed the C++ PR workflow trigger name so workflow_run can resolve it reliably.
- [2026-09-01] Added modular P3 meteorite spell runtime VFX, keeping generated solid assets separate from runtime trail, fire, ember, smoke, distortion, impact, shock-ring, and scorch layers.
## 2026-09-01

### Meteorite spell runtime

Add the modular P3 meteorite spell runtime actor, keeping solid generated assets separate from runtime trail, fire, ember, smoke, distortion, impact, shock-ring, and scorch layers.

- [2026-08-31] V2 Construction now rejects non-finite initial health and damage before they can poison authoritative replicated structure state (#22).

- [2026-08-31] [meta] Hardened shared C++ cohort planning for GitHub Checks rollup shapes, locale-independent timestamps, and empty status filters so merge-ready C++ PRs are admitted consistently.

- [2026-08-31] [meta] Shared C++ cohort planning now grants read-only GitHub Checks access so the trusted workflow token can inspect exact-head check rollups before admitting any integration build.

- [2026-08-31] [meta] Replaced per-PR P3 Unreal C++ builds with cheap admission plus shared compatible-head integration cohorts, so one full Editor build and bundled focused/runtime acceptance validate a cohort instead of a FIFO swarm; build-proof reuse now ignores docs/content-only changes while source/descriptor edits invalidate it (#708).

# Changelog


All notable changes to this project are documented in this file.

The format is based on [Keep a Changelog 1.1.0](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning 2.0.0](https://semver.org/spec/v2.0.0.html).
The rule this file follows is `organicoverlords/docs` ├âãÆ├é┬ó├â┬ó├óÔÇÜ┬¼├é┬Ø├âÔÇª├óÔé¼┼ô├âãÆ├åÔÇÖ├âÔÇÜ├é┬│├âãÆ├åÔÇÖ├â┬ó├óÔÇÜ┬¼├é┬Ø├âãÆ├åÔÇÖ├â┬ó├óÔÇÜ┬¼├é┬í├âãÆ├åÔÇÖ├âÔÇ╣├àÔÇ£├âãÆ├é┬ó├â┬ó├óÔÇÜ┬¼├é┬Ø├âÔÇÜ├é┬╝├âãÆ├åÔÇÖ├âÔÇÜ├é┬┤├âãÆ├é┬ó├â┬ó├óÔÇÜ┬¼├é┬Ø├âÔÇª├óÔé¼┼ô├âãÆ├åÔÇÖ├âÔÇÜ├é┬ó├âãÆ├é┬ó├â┬ó├óÔÇÜ┬¼├é┬Ø├âÔÇÜ├é┬¼├âãÆ├åÔÇÖ├â┬ó├óÔÇÜ┬¼├à┬í├âãÆ├é┬ó├â┬ó├óÔÇÜ┬¼├é┬Ø├âÔÇª├óÔé¼┼ô├âãÆ├åÔÇÖ├âÔÇÜ├é┬│├âãÆ├åÔÇÖ├â┬ó├óÔÇÜ┬¼├é┬Ø├âãÆ├åÔÇÖ├â┬ó├óÔÇÜ┬¼├é┬í├âãÆ├åÔÇÖ├âÔÇ╣├àÔÇ£├âãÆ├é┬ó├â┬ó├óÔÇÜ┬¼├é┬Ø├âÔÇÜ├é┬╝├âãÆ├åÔÇÖ├âÔÇÜ├é┬┤├âãÆ├é┬ó├â┬ó├óÔÇÜ┬¼├é┬Ø├âÔÇª├óÔé¼┼ô├âãÆ├åÔÇÖ├âÔÇÜ├é┬ó├âãÆ├åÔÇÖ├â┬ó├óÔÇÜ┬¼├é┬Ø├âãÆ├åÔÇÖ├â┬ó├óÔÇÜ┬¼├é┬í├âãÆ├åÔÇÖ├âÔÇÜ├é┬┐├âãÆ├é┬ó├â┬ó├óÔÇÜ┬¼├é┬Ø├âÔÇª├óÔé¼┼ô├âãÆ├åÔÇÖ├âÔÇÜ├é┬│├âãÆ├åÔÇÖ├â┬ó├óÔÇÜ┬¼├é┬Ø├âãÆ├åÔÇÖ├â┬ó├óÔÇÜ┬¼├é┬í├âãÆ├åÔÇÖ├âÔÇ╣├àÔÇ£├âãÆ├é┬ó├â┬ó├óÔÇÜ┬¼├é┬Ø├âÔÇÜ├é┬╝├âãÆ├åÔÇÖ├âÔÇÜ├é┬┤├âãÆ├é┬ó├â┬ó├óÔÇÜ┬¼├é┬Ø├âÔÇª├óÔé¼┼ô├âãÆ├åÔÇÖ├âÔÇÜ├é┬ó├âãÆ├é┬ó├â┬ó├óÔÇÜ┬¼├é┬Ø├âÔÇÜ├é┬¼├âãÆ├åÔÇÖ├â┬ó├óÔÇÜ┬¼├ï┼ô `standards/changelog.md`.

## [Unreleased]

- [2026-09-04] [meta] Fixed P3 policy-cutover bootstrap gates: trusted candidate validation no longer requires the retired local policy injector, the PR contract test list parses on Windows PowerShell, and failure receipts publish UTF-8 JSON.

- [2026-09-03] [meta] Reused hydrated editor lanes using measured target LFS growth, removed the obsolete 75-to-100 GiB proactive startup sweep, and stopped the global 8 GiB reserve from overriding smaller measured operations while retaining the hard 25 GiB floor and wrapper-specific conservative reserves (#611).

- [2026-09-03] Bound all six remaining Lane War environment qualification gaps to exact current Tiny3D candidate asset/package identities while preserving existing gameplay geometry and collision (#414).

- [2026-09-03] P3 lane contract fixtures now use the GitHub job-scoped runner temp directory when available, avoiding shared system-temp races during contract validation.

- [2026-09-03] Replaced the legacy grey/brown meteor body and primitive impact placeholders with the generated LowVRAM hero meteor, preserving the generated shock ring and each rubble mesh's imported materials (#603).
- [2026-09-02] Replaced Lane War creep presentation polling with world/actor lifecycle discovery, preserving late-tag attachment without scanning every actor four times per second (#607).
- [2026-09-02] [meta] Generated AGENTS policy projections no longer require product changelog entries.
- [2026-09-02] Simplified P3 worker execution: workers choose highest-value unowned work when no task is assigned, and reuse current-run live facts instead of repeating discovery/status probes.
- [2026-09-02] Documented the measured P3 cleanup/build-lock contract: complete cache inventory, truthful dispositions, effective one-lane hot-source capacity, UBA collision routing, and separation of proactive reserve from hard admission (#296, #599).
- [2026-09-02] Course-corrected the V2 North Star around one continuously runnable default product, an explicit readiness ledger, bottleneck-based WIP limits, default-path smoke, and proof retry/run budgets (#599, #611).
- [2026-09-01] Versioned the 80-slot showcase asset contract from the live source library, hash-binding 48 concrete source candidates while preserving explicit source and polish gaps (#454).

- [2026-09-01] [meta] Restored the exact C++ admission workflow name in the integration-cohort trigger so shared PR validation can start (#206).

- [2026-09-01] Lane War construction blockers now refresh their dynamic navigation state for authoritative and replicated support-loss destruction, reopening tactical routes instead of leaving stale obstacles (#206).

- [2026-09-01] Lane War ridge ramps now share the styled production-ground material while retaining their existing traversal geometry and collision semantics (#414).

- [2026-09-01] Lane War production-world authoring now uses the styled Showroom ground material instead of the neutral prototype surface, improving battlefield readability without changing ground collision geometry (#414).

- [2026-09-01] Lane War production-world dressing now keeps presentation-scale Birch trees at least 1800 uu from the authored V2 player start, preserving spawn readability while retaining non-blocking environment dressing (#414).

- [2026-09-01] V2 Construction can capture and restore stable structure identity, placement semantics, transform, and authoritative health/destruction state for persistence without regenerating PersistentGuid (#22).

- [2026-09-01] V2 retirement: prevent the legacy survival playtest bootstrap subsystem from being instantiated outside its two legacy playtest maps (#611).

- [2026-09-01] [meta] Converged single-machine worker storage around reusable source lanes, one hydrated editor lane, reserved 25 GB disk headroom, safe cold-cache recovery, hash-bound LowVRAM/Tiny3D fan-in, and a merge-stable README changelog link.
- [2026-09-01] V2 Lane War now starts a feature-owned opposing-core wave in the production GameMode, with shared wave-spawn identity, AI navigation opportunity, direct fallback routing, creep combat, and victory-state integration (#607).

- [2026-08-31] Live asset catalog execution now resolves and validates an installed Python 3.11 portably, including uv-managed interpreters, and fails closed when no compatible interpreter is available (#713).

- [2026-08-31] V2 resource interaction now rejects misconfigured finite-resource nodes during target eligibility, so a broken nearer node cannot mask a valid harvest target (#20).

- [2026-08-31] V2 Construction snapped occupancy now scopes repeated anchor names by the supporting structure PersistentGuid, so equivalent sockets on different structures no longer collide in the authoritative placement registry (#22).


- [2026-08-31] V2 now machine-enforces that PlayerController stays out of pawn reselection while Experience/GameMode/PawnData own body selection, preventing the retired V1 cross-controller bird takeover path from returning (#475).

- [2026-08-31] V2 humanoid roster presentation now refuses a different PawnData after the owning Pawn has an authoritative V2 PawnData selection, preventing same-class avatar definitions from silently drifting roster identity or animation presentation (#205).

- [2026-08-31] Canonical visual proof capture now suppresses WebSocket async completion objects, keeping successful screenshot/video output JSON-only for downstream runtime acceptance callers (#458).

- [2026-08-29] Activated the V2 TowerDefense feature composition actor in Lane War server worlds through an idempotent feature-local world subsystem, with replicated defense and wave state owned by the feature actor (#607).

- [2026-08-31] P3 C++ pull requests now use cheap admission and base-specific shared integration cohorts, collapsing compatible merge-ready heads into one full Editor build while preserving focused/runtime and separate visual MP4 acceptance (#708).

- [2026-08-31] Build stall detection now ignores tiny paging-only compiler CPU trickles while still counting action changes and substantive compiler progress, preventing low-memory builds from holding the single P3 build slot indefinitely (#705).
- [2026-08-30] [meta] Added feature-local TowerDefense regression proof that real V2 enemies gate wave advancement and exhausted spawn retries remain visible in replicated defense state (#607).

- [2026-08-31] Worker proof launches now stay hidden, visual-polish acceptance requires the canonical playable MP4 artifact, and merge-facing C++ validation is coalesced into one warm exact-head build cohort instead of one self-hosted build job per PR.

- [2026-08-31] Proof capture callers now route through the canonical `tools/proof/capture.py` primitive, with a fail-closed single-path guard and explicit bridge-session port overrides (#458).
- [2026-08-31] V2 Flight now registers P3FlightProfile and P3FlightBodyProfile under /Game/V2/Flight and adds persisted Hummingbird movement/body Primary Data Assets with procedural-secondary-motion, hover, camera, and shared-flight tuning for #28.
- [2026-08-31] Opposing Lane War cores now spawn facing inward down the lane instead of both facing +X, so the two production base structures read as opposing bases; core health, faction, victory, replication and collision semantics are unchanged, and the #413 core presentation/binding contracts now run in the PR gate (#413).

- [2026-08-30] [meta] Synced shared agent policy v1.25 so temporary branches, worktrees, stashes, and recovery refs are closed instead of accumulating as repository storage.

- [2026-08-30] [meta] Synced shared agent policy v1.24 so fresh workers use the current route-recovery, local-coding bootstrap, and repo-mutation boundaries.

- [2026-08-30] P3 now separates lightweight worker source lanes from the bounded persistent hot Unreal compile pool: source lanes cannot regrow branch-local build state, the hot pool validates local unpushed checkpoint commits, and editor lanes remain explicit for real runtime/asset work.

- [2026-08-29] Construction structures now expose a deterministic player-facing presentation, prove authoritative two-client replication, and run focused/runtime acceptance in the PR C++ gate instead of being skipped (#604).

- [2026-08-28] V2 Construction now lives under the intended `Plugins/P3/Features/P3Construction` feature domain, rejects malformed/non-finite placement definitions, and atomically consumes feature-authored V2 inventory costs exactly once before registering one authoritative structure (#604).

- [2026-08-29] [meta] Synced the shared coordinator identity policy so every mutation claim actor names its harness plus a task/session suffix.

- [2026-08-29] [meta] Synced the shared Vault-continuity policy so fresh coding sessions bootstrap from the Vault while retaining standalone BusyCoordinator authority.

- [2026-08-29] [meta] Trusted multiplayer runtime acceptance now braces `ServerMap` before appending `?game=`, preventing PowerShell from treating `ServerMap?game` as an undefined variable and unblocking PR #678 C++ validation (#584).

- [2026-08-28] V2 production-world authoring now explicitly requests partitioned PCG meadow generation before saving, so the authored meadow marker is not left dependent on implicit load-time generation (#584).
- [2026-08-28] Bound Lane War grounded creeps to the exact qualified Tiny3D identity and replicated skeletal presentation, with live-Python Tiny3D materialization for runtime acceptance (#416).
- [2026-08-31] Current Lane War creeps now receive the qualified Tiny3D mesh and locomotion/combat/damage/defeat presentation through a feature-owned tag bridge, preserving the existing creep gameplay authority (#416).

- [2026-08-28] Source-focused automation now excludes unrelated LFS Content by default, narrow hydration is hash/disk/time-budgeted with transactional pointer restoration, warm-lane planning is non-mutating, and repeated build waits require observed capacity change (#674).

- [2026-08-28] [meta] Removed stale repo-local MCP0 BUSY claim/release authority from the P3 workflow and made coordination CI fail closed on any `canonical MCP0` recurrence.

- [2026-08-28] Added TowerDefense-owned blocking construction that consumes shared P3Construction lifetime/damage while exporting a dynamic NavArea_Null modifier for enemy rerouting under DynamicModifiersOnly navigation (#607).

- [2026-08-28] [meta] Restored the standalone canonical coordinator across shared policy, P3 coordination docs/control metadata, and added fail-closed regression coverage against legacy BUSY authority.

- [2026-08-27] V2 RTS camera mode now follows a replacement possessed Pawn when leaving RTS view after a body swap, while preserving custom base view targets and never changing gameplay possession (#601).

- [2026-08-27] V2 gameplay HUD presentation now composes feature-owned widgets and consumes event-driven survival and wave/core read models inside P3UI instead of extending the V1 central HUD path (#609).

- [2026-08-28] Added a repeatable V2 Earthworks 30-edit performance acceptance runner that records hashed p95/max receipts and rejects any standard game-thread edit over the 100 ms hitch budget (#605).

- [2026-08-28] Bound V2 TowerDefense enemies to the shared P3AI Character/AIController/StateTree substrate so spawned enemies are movable AI pawns instead of feature-local actor-only bodies (#607).

- [2026-08-28] Added one isolated qualified Tiny3D Birch at the north-central production-world perimeter `(0, 12500)`, preserving hash-bound identity and `NoCollision` through the existing qualified placement path (#595).

- [2026-08-28] Added feature-owned V2 TowerDefense tower/enemy combat actors with explicit authority target assignment, shared faction-friendly-fire policy, standard Unreal damage, and replicated health/defeat state without world scans (#607).

- [2026-08-28] Canonical worker visual proof capture now hashes screenshots, videos, keyframes, and contact sheets with streamed .NET SHA-256 instead of relying on unavailable `Get-FileHash` (#664).

- [2026-08-27] Added a V2 `P3TowerDefense` feature composition root that owns the shared replicated defense read model and deterministic wave director without host-GameState or world-scan ownership (#607).

- [2026-08-28] Merge-ready hot validation now maps P3TowerDefense to its focused feature-composition automation inside the existing trusted runner lease, avoiding unsafe post-job workspace races (#662).

- [2026-08-28] V2 world bootstrap now keeps semantic environment state non-spatial in World Partition, reconciles any transient authority fallback into the authored singleton, materializes only the production-world LFS map/partition payload in the hot runner, and proves the resulting singleton across a trusted dedicated server plus two clients in `Lvl_V2ProductionWorld` (#608).

- [2026-08-28] Multiplayer runtime acceptance now reads live Unreal `-abslog` files with shared read/write access, preventing PASS-marker deadlocks while the dedicated server and clients still own their logs (#659).

- [2026-08-28] Headless multiplayer runtime acceptance now disables Unreal Python startup, preventing EditorScriptingUtilities/PythonScriptPlugin crashes before dedicated-server listen while preserving the existing two-client proof contract (#657).

- [2026-08-28] PR C++ validation now runs merge-ready hot acceptance when a draft PR becomes ready for review, preventing ready transitions from reusing a draft-only build that skipped integration/runtime gates (#655).

- [2026-08-28] Merge-ready Survival C++ validation now executes a trusted dedicated-server plus two-client runtime acceptance inside the existing hot-runner lease, requiring server/client PASS markers before checkout release (#653).

- [2026-08-28] Merge-ready hot C++ validation now executes approved focused Unreal Automation tests inside the same runner lease after exact integration builds, preventing post-build workspace cleanup from invalidating runtime acceptance (#650).

- [2026-08-28] Restored the bounded persistent hot-source build pool with fail-closed dirty-lane protection and canonical `Invoke-P3Build.ps1` routing, so preserved asset/map work is never reset or recycled while clean warm state remains reusable (#649).

- [2026-08-28] Warm P3 lane reuse now consults canonical BUSY issue claims before switching a clean worktree and disables recycling when coordinator state cannot be read, preventing active worker lanes from being silently repurposed (#420).

- [2026-08-28] Fixed warm module-first PR validation on Windows PowerShell so multi-module diffs are passed to UBT as separate module names instead of one space-joined invalid module (#296).

- [2026-08-28] Restored warm module-first PR C++ validation: self-hosted checkouts preserve only Unreal `Intermediate`/`Binaries` on one pinned hot runner, draft PRs compile exact changed modules, and merge-ready PRs add one full Editor integration validation (#296).

- [2026-08-28] [meta] PR C++ validation now preserves per-PR stale-head cancellation while serializing builds repository-wide before self-hosted checkout, preventing queued jobs from holding runner workspaces while waiting on the machine build slot (#296).

- [2026-08-28] Worker-owned UE5.8 Editor launches now set `UE_SKIP_UBT_SDK_SETUP=1` only on the child process, allowing concurrent Turnkey ValidatePlatforms checks without blocking worker startup while retaining normal build-lock protection (#573).

- [2026-08-28] Clean attached and detached P3 checkouts now resolve their Git tree identity without disabling build single-flight reuse, preventing validated targets from falling back to redundant full Editor builds (#634).

- [2026-08-28] P3 lane creation now reuses clean, inactive, fully-pushed non-primary warm worktrees before allocating new lanes, preserving warm build caches instead of duplicating multi-GB worktrees (#296, #630).

- [2026-08-28] Restored module-first warm C++ iteration as the P3 worker default: local source changes use `Invoke-P3Build.ps1 -Module <affected-module>` before any full `p3Editor` build, while full targets remain reserved for CI/runtime/proof gates (#632).

- [2026-08-28] Restored the machine-wide P3 build wrapper to one concurrent build slot while retaining the per-build `-MaxParallelActions=2` cap, preventing concurrent builds from saturating RAM and disk under worker load (#296, #629).

- [2026-08-28] Build validation keys no longer depend on PowerShell `Get-FileHash`; the single-flight hash path now uses local .NET SHA-256 so self-hosted runners can validate P3 build inputs before UBT starts (#626).

- [2026-08-27] V2 combat now natively registers feature-owned GameplayCue handlers for Meteor, Chain Lightning, Frost Field, and Repair Pulse so runtime cue dispatch resolves without Blueprint asset registration dependencies (#603).

- [2026-08-27] P3 Tiny3D materialization now accepts hash-bound `ground_skeletal` handoffs, validates their locomotion contract and requested animation, and keeps avian-only FBX/physics requirements out of grounded skeletal imports (#394).
- [2026-08-27] Hardened V2 Earthworks command validation so malformed/non-finite authoritative brush payloads and out-of-contract height steps are rejected before terrain evaluation (#605).

- [2026-08-27] V2 survival now owns authoritative defeat-to-respawn readiness timing on persistent PlayerState state, preserving GAS-backed body reset/protection without extending V1 runtime authority (#606).
- [2026-08-27] V2 combat now natively registers feature-owned GameplayCue handlers for Meteor, Chain Lightning, Frost Field, and Repair Pulse so runtime cue dispatch resolves without Blueprint asset registration dependencies (#603).

- [2026-08-27] Machine-enforced the documented P3 per-build compiler cap by passing `-MaxParallelActions=2` through the canonical build wrapper, preventing a single Unreal build from silently fanning out to all six physical cores under memory pressure (#296).

### Fixed

- [2026-08-28] Shared health now rejects non-finite max-health values before they can poison replicated current/max health state (#484).

- [2026-08-27] Changelog landing validation now decodes Git diff output as UTF-8 on Windows, preventing locale-dependent UnicodeDecodeError failures for valid UTF-8 changelog content (#619).

- [2026-08-27] P3 builds now use a progress-aware stall timeout instead of a fixed total execution deadline, so advancing UBT logs or compiler actions keep healthy long builds alive while genuine no-progress stalls still fail with exit 124 (#523).

- [2026-08-26] [meta] Sync shared policy v1.16 so P3 workers treat exact expensive work as single-flight and reuse valid PASS results instead of independently rerunning it.

- [2026-08-26] [meta] #528 makes the PR C++ validation gate single-flight by exact clean Git tree and UE toolchain, so duplicate requests share one observed build result instead of serially rerunning UnrealBuildTool.

- #572 bounds trusted-control archive downloads, validates ZIP bytes before extraction, and retries fresh downloads fail-closed so transient GitHub archive responses cannot stall candidate policy validation.

- Route incomplete PR handoffs by the first unmet SOURCE/BUILD/AUTHORING/RUNTIME/VISUAL_REVIEW/MERGE gate, with an executable next action and compatibility bundle, and fail unattended editor startup fast when Turnkey ValidatePlatforms is already occupied so proof-heavy work requeues instead of parking at a misleading 90% (#419).
- Replicate authoritative world time, weather, rain intensity, and wetness so multiplayer clients render the server environment state (#512).

### Added

- [2026-08-27] V2 world bootstrap now guarantees one replicated semantic environment-state actor from the P3World subsystem on authority, without map-name or V1 survival bootstrap ownership (#608).
- [2026-08-27] Added a feature-owned V2 humanoid avatar presentation contract with PawnData-selected mesh/animation profiles, stable roster definitions, socket validation, and data-driven roster authoring for Fennec and Sealdiver (#602).
- [2026-08-28] Added a feature-owned V2 combat GAS execution seam that applies faction/damage policy before authoritative mutation and emits the P3Combat gameplay-cue contract (#603).
- [2026-08-27] V2 combat now owns feature-local server-authoritative damage/faction decisions and GameplayCue-driven Meteor, Chain Lightning, Frost Field, and Repair Pulse presentation inside `P3Combat` (#603).

- [2026-08-27] Machine-enforce the V2/V1 architecture ratchet: ordinary PRs may no longer extend protected V1 runtime ownership, while explicit V2-07 retirement issues remain the only cutover exception (#600).

- [2026-08-27] Qualified-world sparse-gap auditing now accepts the canonical seed plus repeated supplement manifests in one read-only pass, so the next Tiny3D placement is ranked against already-proposed standalone placements instead of only the seed (#612).

- [2026-08-27] Added one isolated far-southeast qualified Tiny3D Birch at `(15000, -14500)`, preserving hash-bound identity and the existing `NoCollision` placement contract (#597).
 - [2026-08-27] Gameplay presentation audio now covers the showcase loop with replicated gameplay cues, cook-safe fallbacks, and a 12-second looping authored ambient wind layer (#457).

- [2026-08-27] Added six isolated qualified Tiny3D Birches to the southeast production-world perimeter, including `(13000, -12000)` at 2,692.6 uu minimum supplement spacing, preserving hash-bound identity and `NoCollision` (#591).
- #511 fills the strongest remaining west-north production-world perimeter gap with one qualified Tiny3D Birch at 3,000 uu prior-tree clearance, preserving hash-bound identity and `NoCollision`.
- #511 adds one qualified Tiny3D Birch to the measured west production-world perimeter gap using the existing non-blocking placement path.
- #511 fills the strongest remaining northeast production-world perimeter gap with one qualified Tiny3D Birch at 3,220.2 uu prior-tree clearance, preserving hash-bound identity and `NoCollision`.
- #511 fills the strongest remaining east production-world perimeter gap with one qualified Tiny3D Birch at 3,224.9 uu prior-tree clearance, preserving hash-bound identity and `NoCollision`.
- #511 fills a measured northwest production-world perimeter gap with one qualified Tiny3D Birch at 3,041.4 uu prior-tree clearance, preserving hash-bound identity and `NoCollision`.

- Canonical owned-PIE visual proof receipts now validate V2 PIE world-name normalization,
  scene-content and optional HUD metadata, pixel statistics, SHA-256 identity, and
  non-destructive repeated evidence directories (#419).

- #511 fills the open north production-world perimeter gap with one qualified Tiny3D Birch at 3,275.7 uu prior-tree clearance, preserving hash-bound identity and `NoCollision`.

- #511 fills the strongest remaining measured west-fringe meadow gap with one qualified, non-blocking Tiny3D Birch placement at `(-4250, 3250)`.

- #511 mirrors the strongest residual south-perimeter gap with one qualified Tiny3D Birch at the southwest edge, 3,535.5 uu from the nearest prior outer tree, preserving hash-bound identity and `NoCollision`.
- #511 fills the strongest measured residual south-perimeter gap with one qualified Tiny3D Birch at 3,535.5 uu prior-outer-tree clearance, preserving hash-bound identity and `NoCollision`.
- #511 fills the strongest remaining measured southwest meadow-edge gap with one qualified Tiny3D Birch at 992.5 uu prior-tree clearance, preserving hash-bound identity and `NoCollision`.
- #511 fills three measured sparse meadow-edge gaps with qualified Tiny3D Birches at 921+ uu prior-tree clearance, preserving hash-bound identity and `NoCollision`.
- #511 fills the final two measured at least 1,000 uu interior meadow gaps with deterministic qualified Tiny3D Birches, preserving hash-bound identity and `NoCollision`.
- #511 completes the four-corner production-world perimeter with one deterministic qualified Tiny3D Birch at the southeast corner, preserving hash-bound identity and `NoCollision`.
- #511 fills the strongest remaining measured southwest-interior meadow gap with one deterministic qualified Tiny3D Birch at 1,006.2 uu prior-tree clearance, preserving hash-bound identity and `NoCollision`.
- #511 fills the strongest remaining measured southwest meadow gap with one deterministic qualified Tiny3D Birch at 1,012.4 uu prior-tree clearance, preserving hash-bound identity and `NoCollision`.
- #511 fills the strongest remaining measured interior meadow gap with one deterministic qualified Tiny3D Birch at 1,030.8 uu prior-tree clearance, preserving hash-bound identity and `NoCollision`.
- #511 fills the strongest remaining measured southeast meadow gap with one deterministic qualified Tiny3D Birch at 965.7 uu prior-tree clearance, preserving hash-bound identity and `NoCollision`.
- #511 fills three sparse production-world corner/perimeter gaps with deterministic qualified Tiny3D Birches at 4,875+ uu prior-outer-tree clearance, preserving hash-bound identity and `NoCollision`.
- #511 fills three final measured interior meadow gaps with deterministic qualified Tiny3D Birches at 1,030.8 uu minimum prior-tree clearance, preserving hash-bound identity and `NoCollision`.
- #511 fills the sparse south production-world perimeter with three deterministic qualified Tiny3D Birches, preserving hash-bound identity and `NoCollision`.
- #511 fills three measured residual edge meadow gaps with deterministic qualified Tiny3D Birches, preserving hash-bound identity and `NoCollision`.
- #511 adds three qualified Tiny3D glide-loop avians to the remaining sparse peripheral production-world sky pockets, preserving deterministic hash-bound placement and `NoCollision`.
- #511 adds three qualified Tiny3D glide-loop avians to measured sparse far-north/east production-world sky gaps, preserving deterministic hash-bound placement and `NoCollision`.
- #511 fills three remaining measured meadow gaps with deterministic qualified Tiny3D Birches at 1,074 to 1,142 uu clearance, preserving hash-bound identity and `NoCollision`.
- #511 fills the sparsest measured east-fringe meadow gap with one deterministic qualified Tiny3D Birch at 1,259 uu clearance, preserving hash-bound identity and `NoCollision`.
- #511 fills the final measured central-interior meadow hole with one deterministic qualified Tiny3D Birch at 1,677 uu clearance, preserving hash-bound identity and `NoCollision`.
- #511 fills three measured sparse interior production-world meadow pockets with deterministic qualified Tiny3D Birches, preserving hash-bound identity and `NoCollision`.
- #511 fills a sparse north-interior production-world meadow pocket with three deterministic qualified Tiny3D Birches, preserving hash-bound identity and NoCollision.
- #511 fills three measured residual production-world sky gaps with qualified Tiny3D flap-loop avians, preserving deterministic hash-bound placement and `NoCollision`.
- #511 fills three measured sparse southern production-world meadow gaps with deterministic qualified Tiny3D Birches, preserving hash-bound identity and `NoCollision`.
- #511 adds three qualified Tiny3D flap-loop avians to sparse west/south production-world sky gaps, preserving deterministic hash-bound placement and `NoCollision`.
- #511 adds three qualified Tiny3D flap-loop avians to central production-world sky gaps, preserving deterministic hash-bound placement and `NoCollision`.
- #511 fills a sparse central-north production-world meadow gap with three deterministic qualified Tiny3D Birches, preserving hash-bound identity and `NoCollision`.
- #511 adds three qualified Tiny3D glide-loop avians to the sparse southeast outer sky, preserving deterministic hash-bound placement and `NoCollision`.
- #511 adds three qualified Tiny3D glide-loop avians to the sparse southwest outer sky, keeping the playable meadow clear while preserving deterministic hash-bound placement and `NoCollision`.
- #511 fills the inner southeast production-world meadow with three deterministic qualified Tiny3D Birches, preserving hash-bound identity and `NoCollision`.
- #511 adds three qualified Tiny3D glide-loop avians to the sparse northern production-world sky, preserving deterministic hash-bound placement and `NoCollision`.
- #511 fills three measured sparse production-world sky gaps with qualified Tiny3D flap-loop avians, preserving deterministic hash-bound placement and NoCollision.
- Added a hash-bound Tiny3D placement manifest with 25 deterministic, non-blocking outer-world Birch placements, 109 interior meadow Birches, plus 60 animated qualified avians (including 27 glide-loop sky silhouettes) for the V2 production world, including the qualified Birch wind material binding (#511).
- Added a read-only PR queue-admission gate that blocks new non-emergency lanes when the open-PR budget is exceeded or another lane owns the workstream; lifecycle merge also rejects explicit unproven runtime/visual acceptance (#533).

- Added an auditable `queue-control` bootstrap label for convergence fixes so the admission gate can be deployed without weakening ordinary backpressure (#533).

- Added a phased proof and batching contract so compatible claims can share one exact-head build and one watched short runtime clip while long-duration or hidden claims remain separate (#533).


- Added a fail-closed PR retirement planner and GitHub apply adapter for duplicate/superseded lanes, preserving unique changes and artifacts while holding dirty or incomplete comparisons (#531).
- A fail-closed, dry-run-only PR lifecycle merger that consumes exact-head
  `MERGE_NOW` classifications and explicit required-gate evidence (#530),
  refusing unresolved review requests while respecting repositories without a
  mandatory approval policy.
- A scheduled bounded fleet lifecycle sweep now retires only exact-patch duplicate
  PRs, preserves distinct isolated work, and emits open-PR backpressure/admission
  signals for convergence before new non-emergency lanes are spawned (#532).

- Exposed the canonical changelog on the GitHub landing page and added CI enforcement for changelog and landing-page synchronization.

- Qualified Tiny3D handoffs can now be materialized as deterministic content-hash batches,
  one bounded Unreal import at a time with per-asset results preserved (#511).

- `NORTH_STAR.md`, and a "Go work on p3" pickup loop in `AGENTS.md` so an unqualified "go
  work on p3" resolves to a concrete task without asking.

- `CHANGELOG.md`, adopting the org-wide changelog standard (`organicoverlords/docs` ->
  `standards/changelog.md`). Changes from 2026-08-24 onward get an entry here; history is
  not backfilled.

### Fixed

- The V2 verifier now remains usable for static and receipt-only checks under
  `python -S` when Pillow is unavailable, loading pixel-analysis dependencies only for
  rendered proof capture and resolving the repository root explicitly (#419).

- The trusted PR queue admission workflow now lets already-open lanes continue on
  synchronization or ready-for-review events under backpressure, while still rejecting
  newly opened or reopened non-emergency lanes (#528).

- Worker Editor preflight now fails fast when Unreal's machine-wide `Build.bat` lock is already occupied, instead of timing out after launching another queued Editor (#544).

- Candidate-control validation now accepts the dedicated `p3-secondary` runner label for short trusted authority checks while retaining the legacy self-hosted pool marker (#562).

- `New-P3Lane.ps1` now treats Git exit status, not normal native stderr, as the success boundary for worktree creation while still failing closed on nonzero Git exits (#525).

- Build-cost affordability, consumption, and refunds now fail closed when duplicate resource costs overflow aggregate int32 quantities (#500).

- Build and lane startup now emit a first-class disk-admission diagnostic before expensive queue, UBT, editor, or worktree work, explicitly separating filesystem writability from admission status and reporting safe reclaimable cache capacity (regression-research#33).

- Lane War now reuses disconnected player start slots without reassigning surviving players or aliasing a full two-player allocation (#509).

- The PR contract gate no longer fails with `file or directory not found: s` when a pull
  request's base has exactly one of the changed-area Python test files; the single-element
  test list is now passed to pytest as an array (#504).

### Changed

- [2026-08-26] Densified the partition-aware meadow dressing marker with a second deterministic fine-upright grass tuft while preserving `NoCollision`, partitioning, Data Layer, and HLOD behavior (#584).
- [2026-08-26] Added a dry-olive reed accent to the partition-aware meadow dressing marker at `(265,-190,0)`, reusing the tracked Showroom mesh with `NoCollision` and unchanged PCG/Data Layer/HLOD behavior (#584).
- [2026-08-26] Added a deterministic read-only sparse-gap audit for qualified world placements, ranking candidate XY points by nearest-neighbor clearance with prefix filtering and central-combat exclusion (#581).
- [2026-08-26] Fixed trusted-main queue admission for existing worker lanes (#568).
- [2026-08-26] [meta] Kept documentation/control-plane maintenance in CHANGELOG.md while excluding it from the README project timeline, and fixed same-day project-entry ordering.
- [2026-08-26] [meta] Hardened project-timeline enforcement so direct pushes to main validate HEAD^..HEAD instead of bypassing the dated changelog requirement.
- [2026-08-26] [meta] Removed an accidentally duplicated README timeline block and made the checker reject duplicate timelines or misplaced shared-agent-policy markers.
- [2026-08-26] Filled qualified production perimeter gaps in the production map (#569).
- [2026-08-26] Refreshed generated development history from current merged state (#570).
- [2026-08-26] Hardened trusted-control archive materialization and fail-closed retry behavior (#574).
- [2026-08-26] Filled the west qualified production perimeter gap (#575).
- [2026-08-26] [meta] Made CHANGELOG.md the dated project timeline and moved its latest five entries to the top of the GitHub README with enforced dated-entry checks.
- Short policy, contract, changelog, main-gate, KONE-probe, and generated-history checks now
  target the `p3-secondary` runner pool; duplicate short runs cancel in flight and generated
  history commits do not retrigger their own workflow, leaving `p3-build` for Unreal C++ gates
  (#562).

- Added a read-only, deterministic PR lifecycle classifier that emits one machine-readable
  state per open PR and requires complete diff comparison before marking a duplicate lane
  `SUPERSEDED_CLOSE` (#529).

- PR contract checks and full Unreal builds now use separate jobs, with full builds limited
  to one named build lane while short checks use any available p3 runner (#517).

- Local P3 C++ iteration is module-first through `Invoke-P3Build.ps1 -Module`; occupied build
  capacity is route contention rather than an overall blocker, while the full target
  remains required before CI, proof, or runtime use (#517).

- `AGENTS.md` shared policy raised to v1.2, adding a hard rail on deleting irreplaceable data
  (masters, assets, evidence) after an agent destroyed a set of masters, plus non-blocking
  defaults for disk reclaim, branch prune-on-merge, and full-target builds for anything CI or a
  runtime loads. The visual-proof gate is explicitly excluded from those relaxations.

- `AGENTS.md` shared policy raised to v1.1: a control plane, MCP included, is transport and
  visibility and never permission, so an unavailable connector can no longer be reported as
  the reason work did not start, and BUSY stays authoritative in the GitHub issue title.

### Fixed

- Replicated bird simulated proxies no longer run the local flight movement/state solver over
  CharacterMovement replication, preventing remote flight corrections from fighting local simulation (#28).

<!--
Delete the headings you do not use. Keep this section at the top at all times.

To release:
  1. Rename this heading to `## [X.Y.Z] - YYYY-MM-DD`.
  2. Open a fresh empty `## [Unreleased]` above it.
  3. Update the link block below.

Entry style ├âãÆ├é┬ó├â┬ó├óÔÇÜ┬¼├é┬Ø├âÔÇª├óÔé¼┼ô├âãÆ├åÔÇÖ├âÔÇÜ├é┬│├âãÆ├åÔÇÖ├â┬ó├óÔÇÜ┬¼├é┬Ø├âãÆ├åÔÇÖ├â┬ó├óÔÇÜ┬¼├é┬í├âãÆ├åÔÇÖ├âÔÇ╣├àÔÇ£├âãÆ├é┬ó├â┬ó├óÔÇÜ┬¼├é┬Ø├âÔÇÜ├é┬╝├âãÆ├åÔÇÖ├âÔÇÜ├é┬┤├âãÆ├é┬ó├â┬ó├óÔÇÜ┬¼├é┬Ø├âÔÇª├óÔé¼┼ô├âãÆ├åÔÇÖ├âÔÇÜ├é┬ó├âãÆ├é┬ó├â┬ó├óÔÇÜ┬¼├é┬Ø├âÔÇÜ├é┬¼├âãÆ├åÔÇÖ├â┬ó├óÔÇÜ┬¼├à┬í├âãÆ├é┬ó├â┬ó├óÔÇÜ┬¼├é┬Ø├âÔÇª├óÔé¼┼ô├âãÆ├åÔÇÖ├âÔÇÜ├é┬│├âãÆ├åÔÇÖ├â┬ó├óÔÇÜ┬¼├é┬Ø├âãÆ├åÔÇÖ├â┬ó├óÔÇÜ┬¼├é┬í├âãÆ├åÔÇÖ├âÔÇ╣├àÔÇ£├âãÆ├é┬ó├â┬ó├óÔÇÜ┬¼├é┬Ø├âÔÇÜ├é┬╝├âãÆ├åÔÇÖ├âÔÇÜ├é┬┤├âãÆ├é┬ó├â┬ó├óÔÇÜ┬¼├é┬Ø├âÔÇª├óÔé¼┼ô├âãÆ├åÔÇÖ├âÔÇÜ├é┬ó├âãÆ├é┬ó├â┬ó├óÔÇÜ┬¼├é┬Ø├âÔÇÜ├é┬¼├âãÆ├óÔé¼┼í├âÔÇÜ├é┬║├âãÆ├é┬ó├â┬ó├óÔÇÜ┬¼├é┬Ø├âÔÇª├óÔé¼┼ô├âãÆ├åÔÇÖ├âÔÇÜ├é┬│├âãÆ├åÔÇÖ├â┬ó├óÔÇÜ┬¼├é┬Ø├âãÆ├åÔÇÖ├â┬ó├óÔÇÜ┬¼├é┬í├âãÆ├åÔÇÖ├âÔÇ╣├àÔÇ£├âãÆ├é┬ó├â┬ó├óÔÇÜ┬¼├é┬Ø├âÔÇÜ├é┬╝├âãÆ├åÔÇÖ├âÔÇÜ├é┬┤├âãÆ├é┬ó├â┬ó├óÔÇÜ┬¼├é┬Ø├âÔÇª├óÔé¼┼ô├âãÆ├åÔÇÖ├âÔÇÜ├é┬ó├âãÆ├åÔÇÖ├â┬ó├óÔÇÜ┬¼├é┬Ø├âãÆ├åÔÇÖ├â┬ó├óÔÇÜ┬¼├é┬í├âãÆ├åÔÇÖ├âÔÇª├óÔé¼┼ô one line, for a human, with the issue or PR number:
Entry style ├óÔÇØ┼ô├â┬Â├óÔÇØ┼ô├â┬º├óÔÇØ┼ô├âÔÇÜ one line, for a human, with the issue or PR number:
  - Sprint combat effects are now server-authoritative; clients can no longer apply
    damage locally (#494).
-->

[Unreleased]: https://github.com/organicoverlords/p3/commits/main
