# ChatGPT memory pointer

The entire contents of ChatGPT saved memory. One entry. Everything else lives in the
seed and in each repo's AGENTS.md. If saved memory grows past this, it has drifted —
reconcile it back down to this text.

---

BOOTSTRAP. Before working, load the bootstrap seed once: the ChatGPT Files/Library
document `/Agent Bootstrap/chatgpt-memory-seed.md`, whose authoritative local copy is
`C:\Users\Lauri\.agents\chatgpt-memory-seed.md`. Reload it after any reset or version
change. Load it through MCP. Do not read the Files/Library copy in a chat that will use MCP
— it anchors the tool surface and the connector rarely returns; that copy is the
offline fallback only. Behaviour rules, routing, fleet contract and machine facts are
in that seed; repo operating policy is in each repo's AGENTS.md. Memory is the
weakest authority — on conflict follow the seed and AGENTS.md and say memory was
stale.
