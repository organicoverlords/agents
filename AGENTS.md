# Shared agent work contract

Shared contract version: 83

- **Shared-rule serving convergence is one-minute maximum.** `C:\Users\Lauri\.agents` must be checked against `origin/main` at least once per minute by the installed headless `AgentRulesCheckoutSync` task. The task may only use the fail-closed default `Sync-AgentRulesCheckout.ps1` path: clean-behind may fast-forward; dirty, ahead, diverged, wrong-branch, or error states remain untouched for explicit attribution/repair. Never stretch this cadence to batch convenience, and never put Git fetch/convergence inside bootstrap itself.

Every agent uses this same repo-work contract. Local repo `AGENTS.md`/`CLAUDE.md` files only point here and to `RULES.md`.

## Entering a repo
- Apply the `RULES.md` **research-deep/output-lean direct-reply gate** before returning a direct user answer: investigate to the depth needed for correctness, refresh materially changeable facts from proposition-appropriate current/live evidence, then compress the final reply to the result and only explanation/caveats that change understanding or action. Do not expose tool chronology, evidence inventories, internal audit steps, or research volume merely because they were needed to reach the answer.
- **Canonical discoverability gaps are repaired at the owner.** When a bounded lookup shows that an expected alias, keyword, trigger, or other navigation term is missing from a canonical navigation/index/search surface where it should resolve, first verify the existing canonical owner from current evidence. If the term legitimately belongs there and the change is cheap, reversible, and collision-safe, add the missing mapping at that owner plus the narrowest regression check as part of the current work. Fail closed when ownership is ambiguous, the absence is intentional, or the surface is external/read-only; never create a parallel index, registry, or authority merely to make a lookup succeed.
- `RULES.md` and `AGENTS.md` carry the same monotonic `Shared contract version: N`. Any shared-contract change to either file increments both version headers in the same patch; bootstrap must report that exact coherent version.
- Before any stack/MCP/infra mutation, apply the `RULES.md` evidence-sufficiency gate: reuse the fresh bootstrap's bounded current topology/recovery orientation, then use one targeted `stack_atlas.py find <natural-language-query>` when owner/WIP/runtime/history identity is unknown. Load full Stack Atlas inventory only when bootstrap plus targeted discovery still cannot bound the cross-component dependency/blast radius. Never infer stack topology from one subsystem file, service, search hit, component lookup, or derived projection.
- Fresh chats that materially need machine state follow the `RULES.md` **first-machine-action bootstrap** rule once. The first machine action is MCPv4 `read_output` with reserved stable `process_id="bootstrap"`, `max_chars=32000`, and `wait_ms=0`; never persist or substitute a runtime UUID for that alias. If the alias is unknown/exited, incomplete, stale beyond 90 seconds, or reports `bootstrap.status` other than `OK`, use a bounded MCPv4 `start_process` fallback for `bootstrap-glance`; when the required binding/schema/authorization/route is missing, stale, disconnected, or transiently rejected, refresh/re-discover only that binding and retry the smallest supported bootstrap action when refreshed evidence could materially change the result. Never tight-loop an unchanged failure; another retry requires changed route/binding/authorization evidence or a natural recovery boundary. A fresh degraded envelope proves only a current refresh failure and its `UNKNOWN`/carried-forward sections are not live truth. Producer file age, Task Scheduler results, or direct producer success do not certify the alias; any bootstrap producer/scheduler acceptance must read `process_id="bootstrap"` itself and verify returned freshness/status/end-marker evidence. After MCPv4 is unavailable/`DEGRADED` or state-aware MCPv4 recovery still cannot obtain bootstrap, the next supported usable route may obtain it; later route retries are allowed only on materially changed route/binding/authorization evidence or a natural recovery boundary. If that next route is Remote Desktop Commander, read `C:\Users\Lauri\Desktop\vault\.state\bootstrap\latest.json` directly with no discovery/search; accept only a complete `bootstrap.v1` with `bootstrap.status=OK` and age <=90 seconds. If it is stale, invalid, or missing, report bootstrap unavailable/stale for that route and do not automatically run `bootstrap-glance`, restart services/tasks, search the filesystem, or perform recovery; refresh/repair requires an explicit user request or a separately established task that specifically authorizes that action. Do not search for bootstrap/MCPVisual when the canonical paths are already known. Follow-ups reuse the snapshot unless changed live machine/route state materially affects correctness; do not make bootstrap, GitHub, Busy, or repo enumeration a per-subtask ritual. Treat bootstrap as orientation only: it owns current machine/route/topology/recovery/live-swarm/source-freshness signals, not exhaustive commits, branches, GitHub history/comments, MCP log history, worker-report history, artifacts, or Vault chronology. Those discovery classes belong to `stack_atlas.py find`.
- Apply the `RULES.md` exact-fact proof gate before status or factual claims. For a factual question, tool choice follows the proposition that must be proved, not whichever snapshot is easiest to obtain. In particular, never substitute current liveness for historical continuity, never substitute process existence for route usability, and never substitute test/merge state for deployed runtime behavior. When direct evidence does not cover the requested proposition and time window, report `UNKNOWN` rather than answering a nearby easier question.
- For **live/current work truth**, apply the `RULES.md` MCP-grounding gate: fresh MCP activity evidence is mandatory first for what is happening now; relevant local repo/worktree/runtime state is mandatory supporting evidence when a local execution surface exists. GitHub, Busy, reports, leases, scheduler state, and static local state may corroborate but never substitute for MCP-grounded liveness. If relevant MCP activity coverage is stale or unavailable, the live/current proposition is `UNKNOWN`.
- Apply **evidence-before-clarification** to factual work. Resolve evidence locators from the conversation and available tools rather than asking the user to repeat discoverable facts. If one bounded lookup can disambiguate the proposition, perform it before asking. Ask no more than one targeted clarification, and only when materially different propositions remain that the available evidence cannot distinguish. A clear proposition with unavailable proof yields `UNKNOWN`; it does not yield a request for the user to do the agent's evidence-gathering work.
- Apply the shared **contradiction-reconciliation gate** whenever relevant evidence disagrees. Identify which source is authoritative for the exact proposition and time window, distinguish event time from observation time, and prefer a bounded origin lookup over another cache/dashboard read. Derived snapshots accelerate retrieval but never gain authority over their origin. Do not hide a contradiction by citing only one side. If authoritative evidence remains genuinely inconsistent or coverage is insufficient to resolve it, report `UNKNOWN`/`CONFLICT` with the scoped competing facts instead of choosing the newest, easiest, or most convenient answer.
- Preserve **entailment strength** in factual answers. Do not promote `INFERRED` conclusions to `OBSERVED`/`ENTAILED` facts. A result may be stated definitively only when an authoritative source directly reports it or it follows necessarily from directly supported premises. Temporal sequence, correlation, replacement, disappearance, startup, health, and successful tests each prove only their own scoped propositions unless an additional bridge is independently established. For stronger yes/no propositions that remain unproved, answer `UNKNOWN` first; optional weaker observations or labeled inferences may follow but must not be presented as the answer to the stronger proposition.
- Keep **observation-channel state separate from target state**. Tool/transport/query failure is `OBSERVATION_FAILED`, not evidence that the target is dead/missing/absent. Before a negative target-state claim, obtain independent authoritative target evidence when supported; otherwise answer `UNKNOWN`. Never use repeated identical probe failures as accumulating proof, and never make recovery polling a substitute for target evidence.
- Preserve **quantifier coverage**. One authoritative witness proves an existential positive (`ever`, `at least one`); stop once the exact yes/no existential proposition is proved unless the user also asked for enumeration. Universal/negative/exact-total claims (`all`, `every`, `none`, `never`, `exactly N`, `how many`) require exhaustive relevant coverage and identity/deduplication where needed. Incomplete coverage yields `UNKNOWN` for the total/universal/negative claim plus only a labeled lower bound or scoped observed count.
- Preserve **identity-layer fidelity**. Do not use evidence about a PID/process instance to answer a logical-service question, endpoint health to prove session/process continuity, launcher activity to prove child readiness, or route/session failure to prove target-service death unless the cross-layer identity/causal bridge is independently established. For lifecycle words such as `again`, `restarted`, `replaced`, `died`, `killed`, or `remained up`, bind observations to the same stable entity at the identity layer the user asked about. Missing identity mapping yields `UNKNOWN` at that layer, with only directly proved lower-layer facts stated separately.
- Machine-route choice follows **operation usability**, not service existence. A live listener or HTTP 200 does not pin work to MCPv4 after a bounded representative operation proves it degraded. Apply the `RULES.md` degraded-route rule, then move to the next supported route instead of reissuing slow MCP calls. For Remote Desktop Commander, `-ReadyProbe` proves only persisted authorization readiness; readiness is account-bound to the locally pinned expected Commander identity, so a structurally valid token for another/unreadable account is not ready. Call the route usable only after successful connection/tool evidence, and never trigger browser authorization during unattended fallback; explicit interactive authorization must reject a newly persisted mismatched identity.
- **Pixel-inspect visual outputs before handoff.** For every image/video, render, UI screenshot, map/layout, 3D reconstruction, point-cloud/mesh, geometry preview, or other visually judged result, inspect the actual pixels/frames of the exact candidate before showing it to the user or calling it successful/usable/correct. Metrics, logs, point counts, reprojection error, tests, hashes, file existence, and producer success are supporting evidence only and never replace or overrule the pixel check. A visibly bad result is `REJECTED`/`NOT_PROVEN` even when metrics pass; if the media cannot be inspected, the visual claim remains `UNKNOWN`/`NOT_PROVEN`. The user is never the first visual reviewer.
- **Reuse already-exposed media directly.** Keep machine-local path, MCP/Library identity, current-session exposure, and `/mnt/data` materialization separate. If the exact asset is already attached/exposed to the current model/session, present and inspect that exposure directly: do not re-upload it through MCPVisual, create a new `/mnt/data` copy, invoke Python/PIL/matplotlib merely to display it, re-encode it, or manufacture another derivative/handle for presentation. Do not automatically cross the machine -> ChatGPT boundary for review. `MCPVisual.upload_local_file` is permission-bearing manual emergency transport only after explicit user approval for that local-only file. Transfer success, a file ID, path, hash, or Library record does not prove that the user currently sees the asset; never claim visibility without direct presentation evidence.
- **Give retained visual proof durable identity at completion; expose it to chat only when needed.** On first asset-render completion, explicit asset-batch completion, or live/editor/PIE/live-coding capture completion, preserve the exact original media in the owning canonical durable proof/asset store and its stable lookup identity when that pipeline already provides one. Do not manufacture a second review store and do not package visual originals into transient ZIPs for ChatGPT. For later review, reuse current-session exposure first; otherwise reopen the canonical durable Library/Files original by stable identity. If durable publication is missing, repair/backfill it at the owning producer/library store. `MCPVisual.upload_local_file` is permission-bearing manual emergency transport only after explicit user approval for that one local-only transfer; it is never an automatic review fallback. Session expiry is a retrieval event, not a reason to regenerate or redesign transport.
- **Review 12-view proof from canonical originals, never archive transport.** If an original/highest-resolution producer 12-view sheet exists, inspect that exact sheet: reuse current exposure or reopen its durable Library identity. If publication is missing, backfill the exact sheet through the owning durable store before review; do not automatically invoke `MCPVisual.upload_local_file`. If no producer sheet exists and the canonical evidence is twelve individual originals, reopen those durable originals directly and backfill missing durable publication at the owner; do not ZIP them and do not invent a reviewer-authored contact sheet. Never substitute thumbnails/reduced previews or alter background, exposure, lighting, crop, scale/resolution, tone/color, or layout for review convenience. Any verdict made only from reduced derivatives while the original existed is provisional until the original is reviewed.
- Apply the `RULES.md` protected-control-plane termination rule before any process/service/task kill or restart whose target could be MCP or Remote Desktop Commander infrastructure. Observation failure is route evidence only; it is never a kill signal. Resolve the target identity and descendant/parent blast radius first, preserve fallback/recovery paths, and use failover or automatic reconnect when possible. If the target may contain protected infrastructure and that mapping is not positively known, do not terminate it. Disposable task-owned children may be killed only after that exclusion is established.
- When a fresh-chat bootstrap snapshot was obtained, inspect `source_freshness.canonical_checkout`. If bootstrap was unavailable because no usable route existed, do not force the suspected/degraded route merely to obtain source-freshness metadata; when serving-checkout correctness materially matters, establish it through the selected usable route or direct repository evidence, otherwise mark that freshness fact `UNKNOWN`. If `source_freshness.canonical_checkout` is available and `coherent` is false, run `C:\Users\Lauri\.agents\Sync-AgentRulesCheckout.ps1` once before selecting repo work or relying on the serving checkout for current shared behavior. An exact `head_matches_remote_main` false value while `coherent` is true is diagnostic cache-lag evidence only and does not require sync. The default sync may fast-forward only a clean behind `main`; `DIRTY_BLOCKED`, `AHEAD_BLOCKED`, `DIVERGED_BLOCKED`, or other failure stays fail-closed. Do not add Git fetch/convergence work inside bootstrap itself, create a watcher/scheduler, hard-reset the serving checkout, or continue as though stale local policy were current. Attribute the blocking state and use the existing preserve-then-converge `-Repair` path only when serving convergence is actually required and exact collision/foreign-work safety permits it.
- **Task-scoped memory is conditional drill-down evidence, not startup ceremony.** When a **specific historical unknown**—such as a prior decision, correction, failed approach, compatibility constraint, or owner/path fact—can materially change the next action and its identity/location is not already resolved, use one unified `stack_atlas.py find <natural-language-query>` pass first. For an exact `mem-*` identity, resolve it with `stack_atlas.py find <mem-id>` so the effective local journal, locally fetched `origin/memory/live` replica, and returned lineage can recover a historical object even when the current checkout seed no longer contains it; preserve its historical/source semantics and never promote it to current truth. Use `memory_bank.py context <query>` or `memory_bank.py timeline <query>` only as second-stage drill-down after `find` surfaces a relevant thread, or when unified discovery explicitly reports insufficient/unavailable history coverage and that missing history can change the next action. Reuse the current conversation/task context, current repo/runtime evidence, and known checkpoints first; if they already establish the needed history, proceed without a memory round trip. Task category alone does not trigger memory, and memory/timeline must never become a per-tool or per-subtask ritual. Current facts still require proposition-appropriate current repo/runtime evidence. Stack/MCP/infra work retains the evidence-sufficiency gate below.
- Immediately before the first mutation in a target repo, inspect that repo's branch/HEAD and dirty state. Reuse the known matching issue/PR/current-plan identity when already established. If matching WIP identity is materially unknown or may have changed, use one `stack_atlas.py find <natural-language-query>` discovery pass rather than manually searching local branches, GitHub, Vault, reports and logs in sequence; then drill into only the returned exact source whose current fact is decision-relevant. Fresh chat/session status alone is not a reason to query GitHub. Acquire only the exact Busy scopes needed with atomic `claim`. Do not run `inspect` before a normal claim: the atomic claim is the collision check. Use `inspect` only after a collision/rejection when owner attribution, stale recovery, or exact claim state is needed. If relevant dirty work already exists, attribute and preserve it rather than piling unowned edits onto it. Repository-content grep/recursive scans are not a discovery fallback; after an exact implementation scope is known, targeted source/caller tracing remains normal engineering work.
- Canonical serving/main checkouts are not runtime-state sinks. Generated or derived refresh helpers default to `%LOCALAPPDATA%` or another non-repository state path; repository output requires explicit artifact intent and an isolated worktree when tracked state may change.
- `C:\Users\Lauri\.agents` is the serving checkout for shared behavior, not a worker WIP surface. Before mutating `organicoverlords/agents`, run `Sync-AgentRulesCheckout.ps1`: a clean behind `main` fast-forwards to current `origin/main`, while dirty/ahead/diverged state fails closed. Do implementation in a separate worktree based on current `origin/main`; never copy partial merged policy/files back into the serving checkout. When serving convergence is explicitly required, `Sync-AgentRulesCheckout.ps1 -Repair` first preserves the exact index/worktree on a local `preserve/agents-live-*` branch, then converges to `origin/main`; do not hard-reset or clean the serving checkout by hand.
- Human-facing repository documentation and product direction are centralized in `organicoverlords/agents@main` under `docs/repos/<repo>/`. Product-repo `README.md`, North-Star paths, `docs/**/*.md`, and similar legacy locations are pointer surfaces only and must not carry competing prose. Read the smallest current Agents-repo documents needed for the task, then use the product repo only for live code/runtime facts, machine-consumed contracts/schemas/fixtures, exact scripts/help, and relevant open work. Artifact-bound evidence/provenance notes stay with their artifacts. Do not refresh, overwrite, or clean dirty product work merely to read documentation.
- Before structural or architectural work, recover prior intent/convergence only when a **specific unresolved historical design or compatibility question** can change the proposed structure. Reuse current task context, explicit user direction, canonical current documentation, and already-held repo evidence first. Task memory/timeline or branch/commit history is conditional on that named unknown; do not require all of them merely because the work is architectural/structural. Do not reconstruct architecture from the current file tree alone when intent is genuinely unknown, but do not perform history archaeology when current authority already establishes the intended state.
- **No partial-understanding stack mutation, but no fixed ceremony.** Complete the shared `RULES.md` evidence-sufficiency gate before a stack/MCP/infra mutation: owner, entrypoint, exact affected dependents/resources, relevant current topology, recovery/rollback path, and only the historical constraints that can materially change the approach must be established coherently. Reuse already-held direct evidence and the fresh bootstrap first; when any owner/WIP/runtime/history identity is unknown, use one unified `stack_atlas.py find` pass. Use full Atlas inventory only when bootstrap plus targeted discovery still leaves the dependency/blast radius materially unknown. Task memory/timeline is drill-down only after a relevant historical thread is surfaced or a concrete historical unknown remains. If the target route is degraded, use an independent supported evidence source rather than making the broken component certify its own repair. If a required field cannot be established, do not mutate it. Understanding one local file/service is not sufficient stack understanding when broader resources are in the blast radius.

## Swarm-friendly issue lifecycle
- **One issue, one outcome.** Use the existing GitHub issue as the shared work/convergence record. A parent convergence issue composes a milestone across several owners; child issues own bounded implementation or proof. Do not turn issues into a queue, worker assignment system, or one issue per checklist row.
- **Material work gets a durable owner early.** For a material, actionable swarm concern that may require coordination, revisit, implementation, proof, or handoff, use the smallest decision-relevant duplicate/owner lookup and create or reuse the canonical issue before deeper implementation when no owner exists. This is not issue-per-observation: keep hypotheses, measurements, sub-findings, and implementation detail on the existing issue unless they become independently actionable outcomes. Preserve one traceable lifecycle from idea/problem through design/diagnosis, patch/PR, validation, integration, and live proof or explicit disproval/supersession; reports and artifacts must link to that owner rather than becoming shadow backlogs. Durable ownership is not worker ownership: creating/reusing an issue must preserve simultaneous entry, disjoint mutation scopes, review/proof/integration by other workers, and switching away from a blocked contribution; never make issue-first lifecycle a single-worker lane or queue.
- **Populate the current plan for simultaneous entry.** Put a compact current-plan block before long detail: authority/goal, existing implementation/PRs/checkpoints, remaining required results, and for multi-contributor work a small table or equivalent that names each required result, existing WIP/owner, exact logical files/resources and serialization boundary, acceptance evidence, and patch -> proof -> integration handoff. Expose multiple real non-overlapping contribution boundaries only where they genuinely exist.
- **Enter by reconciling, not inventing.** On a fresh issue entry with no usable task context, read the compact current-plan block, linked WIP/checkpoints, and only recent material comments needed to identify changed direction once. When that context is already loaded for the coherent task, reuse it and reconcile only evidence that may have materially changed the next action; do not reread a long issue body/comment history per subtask. Prefer reuse/review/proof/integration of matching work and select a new output only for an uncovered acceptance gap. At a meaningful checkpoint/handoff, record branch plus immutable commit when available, validation already performed, unresolved blocker/finding, and next integration action; no heartbeat comments or duplicate status notes.
- **Update issues by the same logic used to create them.** Evidence-only progress belongs in a dated comment. A material scope, priority, acceptance, or direction change updates the issue's current-plan block so the next worker does not have to reconstruct direction from comment archaeology. Treat concurrent edits to the issue body as a shared serialization boundary under the existing exact-resource collision rules; comments may remain concurrent evidence.
- **Route user direction to the smallest existing authority.** A local instruction changes the affected issue/WIP only. A milestone or priority change updates the parent convergence issue and reconciles affected children/WIP. A real product-direction change updates the canonical North Star/direction document first, then the parent convergence issue. The worker that receives a material user-direction change owns this reconciliation before continuing; current explicit user direction outranks stale prose.
- **No orphaned work.** Before completing a material current-plan update, account for every active child issue, PR, branch/commit checkpoint, or proof artifact named by the previous plan. Each must be retained, retargeted with its still-valid contribution, blocked with a concrete gate, superseded by an exact successor/canonical path, or closed. Never silently drop useful WIP because the plan changed; preserve unique commits/artifacts and link the successor when work is superseded.
- **Issue-wall recovery is worker-owned.** If the current issue/parent appears exhausted, stale, overconstrained, or has no ready contribution, do not ask the user to seed, curate, reprioritize, or repair the issue graph. Reconcile the current North Star/parent acceptance against live main/runtime evidence, linked PRs, checkpoints, and existing children. Close or supersede finished/duplicate paths, retarget still-valid WIP, refresh the parent current plan and Project status, and create a new bounded child issue only for a real uncovered acceptance gap with no existing owner. If the parent acceptance is actually satisfied, close/advance it and continue from the next existing North-Star priority. Do not change the North Star merely to manufacture work; change product direction there only when explicit user direction or a genuine authority correction requires it.
- **Project active delivery into Stack Delivery.** `organicoverlords` Project #1 (`Stack Delivery`) is the existing cross-repo delivery projection; the owning issue/PR remains the work authority. Add active tracked delivery issues that matter to the current objective and keep `Status` current at meaningful plan/handoff/merge/proof boundaries: `Idea` = candidate only, `Shaping` = goal/acceptance still being resolved, `Ready` = accepted and actionable with no current WIP, `Active` = implementation/integration underway, `Review` = an exact patch/PR is awaiting review or landing, `Proof` = implementation has converged and acceptance proof remains, `Blocked` = that issue outcome cannot currently advance because of a concrete gate, and `Done` = closed/accepted. A parent may remain `Active` while one child is `Blocked`. Never use project status as ownership, admission, scheduling, or a reason to wait.
- **Use GitHub-native relationships as navigation.** When an issue is genuinely a bounded part of a parent convergence issue, record it as a native sub-issue so Project #1's `Parent issue` and `Sub-issues progress` fields work automatically; keep the parent current-plan table because it explains boundaries and acceptance. Pull requests reference their owning issue so `Linked pull requests` is useful; do not add every transient PR as a project item unless the PR itself is a cross-repo delivery object.
- **Use labels and milestones semantically, never as worker state.** Prefer a small stable label vocabulary for product/domain/type/risk such as `gameplay`, `acceptance`, or `performance`; do not encode worker identity, liveness, Busy ownership, or temporary assignment in labels or assignees. Use an existing repository milestone only when it genuinely names the delivery milestone; do not invent or force a milestone merely to mirror Project status. When a material plan change alters these semantics, reconcile the Project/relationship/label projection in the same update so it does not become stale.
- **Sanity-check convergence, not permission.** Before expanding scope or creating new WIP, compare the requested direction with current main/runtime evidence and the parent acceptance result: is the actual product/system getting better, does existing WIP already cover the gap, is the current bottleneck implementation or proof/integration, and can two simultaneous entrants choose useful nonoverlapping outputs and know what must serialize? Repair a stale plan when needed. This is a bounded design check, not a new approval gate or mandatory orchestrator.
- **Handoff and close cleanly.** The current exact mutation claimant composes overlapping contributions into one coherent patch. Define the handoff from patch to proof to integration with an explicit result/reference. Verified useful work should converge through the normal PR/integration path; stale duplicate paths are closed/superseded instead of becoming shadow backlogs.
- **Small issues stay small.** An ordinary one-owner issue can use compact bullets for Goal, Existing WIP, Acceptance, and Next handoff. Add the fuller contribution map only when several workers or owners genuinely need it.

## Doing the work
- Apply the `RULES.md` **foreground user-session interaction** gate to every automation path. Background/headless automation is normal. Before GUI/desktop automation that may visibly disturb the user session—mouse/keyboard injection, focus changes, visible browser/tab navigation, or visible window manipulation—send the required concise notice naming the application/window and visible effect. After that notice, ordinary non-destructive foreground interaction that materially advances the established task may proceed without separate confirmation unless another rule requires it. An explicit user prohibition on that application/window/action class remains a hard block until explicitly reversed.
- For tracked engineering work that already uses an issue, use that issue as the shared coordination record and work identity, and reference it from meaningful branches/PRs/commits/checkpoints. A bounded local read/write/update or other prompt-local operation does not require creating or touching a GitHub issue merely for bookkeeping.
- A lane, branch, worktree, checkout, editor, build slot, route, or Busy claim is an execution surface/resource, not worker identity and not the task. Many agents may contribute to the same issue concurrently.
- Before each shared mutation, claim only the exact files/resources being changed through BusyCoordinator; do not turn the issue, lane, checkout, or Busy into a repo-wide lock.
- Give each Busy actor a unique approved-harness plus task/session suffix; never reuse a date-only or generic recovery actor across concurrent workers. While an exact claim is still needed, heartbeat it at least every 120 seconds with a fresh operation ID because the coordinator lease is capped at 240 seconds. Expiry releases collision ownership only and must not be treated as task completion or cleanup permission.
- Put active WIP worktrees in a durable repo-owned root or %LOCALAPPDATA%\AgentWorktrees\<unique-id>. Never create active WIP under %TEMP%, npm/npx cache, report-reaper launch roots, or other lifecycle-cleaned scratch paths; those locations are for disposable state only.
- Busy mutation idempotency is per semantic mutation: never reuse one `--operation-id` across different claim, release, heartbeat, or recovery operations/scopes. Reuse an operation ID only to deliberately replay the same semantic operation. If key reuse causes a partial multi-scope result, reconcile the scopes that actually registered, clean up only owned residue, and fix the caller/helper so the next worker cannot repeat the same trap.
- Busy stale/orphan reclaim is routine. `inspect <exact-scope>`; if the owner is positively attributable, has no live child process, and has had no MCP process activity for at least 60 seconds, CAS-recover with the inspected old actor and exact timestamp: `busy-python.cmd recover <old-actor> <scope> --expected-claim-timestamp <timestamp> --operation-id <unique-id>`. `claim_changed` or newer activity cancels recovery. On success, claim the same scope under the current actor with a new operation ID and continue. Never rediscover this procedure during live work.
- If the current execution surface is dirty, stale, unsafe, unusable, or contains foreign/unique work, preserve it and leave the conflicting state intact. Continue through another already-existing safe supported surface or other useful work that materially advances the user's established goal. A decision-ready blocker is not a valid yield merely because no ready in-scope contribution remains; only the actual stop conditions in `RULES.md` permit yield. Do not manufacture unrelated work merely because the current surface cannot safely mutate.
- Do not create a fresh lane/worktree reflexively to escape an inconvenient current surface. Reuse safe existing supported surfaces and preserve useful warm state.
- Choose the highest-value load-bearing action that advances the current user objective. Existing unfinished relevant WIP should be repaired, proved, integrated, closed, or superseded before avoidable parallel WIP is created. Do not sit on CI, review, a Busy claim, a build slot, or another worker when another ready required contribution can advance the same established objective.
- Reuse the repo's existing supported entrypoints and wrappers. If a supported path required by the current request is broken, fix that owner rather than bypassing it with a parallel path; do not turn an unrelated broken path into work merely because it was discovered.
- Debug owner-first, not search-first. When the owner/entrypoint is known, use the exact error plus the smallest owner contract/source to form one hypothesis and one discriminating check. Do not fan out into wrapper/runbook/task/filesystem archaeology unless that specific unknown remains unresolved. A rejected low-level call does not justify inventing a parallel recovery path.
- A heavy wrapper is not current safety authority merely because its filename is canonical. Before build/runtime/render/GPU launch from a worktree that may be stale, resolve the repo's current supported heavy entrypoint from an already-existing refreshed execution surface; if it can target another project/worktree root, run that current wrapper against the preserved target instead of the stale branch-local copy. If current safety ownership cannot be established, fail closed; continue only other work already required by the current request, otherwise yield. Never refresh, overwrite, or clean foreign/dirty target state just to make the wrapper current.
- Make coherent changes and validate with the cheapest evidence sufficient for the requested claim. Do not poll unchanged state. A queued CI/build/check that repository policy does not require for merge or the requested proof is not a wait gate when current local/repo-owned verification is sufficient; use the supported integration path when otherwise eligible. Required checks, repo-owned merge guards, and proof required for the requested claim remain gates. When a required external/build/CI action is in flight, leave a checkpoint if needed and advance another ready contribution; return to the in-flight result only when it can change the next action. Do not manufacture unrelated adjacent work merely to keep a manual turn alive.
- Before sending an MCP `start_process` command, normalize obvious PowerShell command-shape hazards instead of relying on preflight rejection: never pipe a `foreach`/`for`/`while`/`if`/`switch` statement directly (capture it first with `@(...)` or a variable), never assign to the read-only automatic `$PID` variable (PowerShell variable names are case-insensitive, so `$pid`/`$Pid` are the same variable); `$args` is writable and may be assigned when that overwrite is intentional, and avoid deeply nested one-line quoting/delimiters when a temporary `.ps1`/`.py` script is safer. For nested PowerShell/Python payloads that would otherwise re-enter quoted `-Command` parsing, use `C:\Users\Lauri\.agents\Invoke-LiteralScript.ps1`; it writes the payload to a temporary script and transports arguments through JSON, so do not fall back to re-embedding the same payload in another quoted command string after a shape failure. For local Python helpers on Windows, prefer the PowerShell call-operator form `& python.exe 'C:\path\tool.py' ...` with each path/argument quoted separately rather than a bare `python <path> ...` command. Do not retry an unchanged preflight-rejected command: make one materially normalized retry when the operation is still required. If the normalized equivalent succeeds, preserve the rejected and successful command shapes as a `tooling` finding in the active worker report when one exists. If the user corrected the command-shape failure, apply the answer-first correction rule; do not create a separate Vault write merely for this tooling finding. Repeated recurrence is an owned tooling defect, so fix the command-producing guidance/helper and add the narrowest available regression check instead of silently bypassing it. Treat syntax/shape preflight failures as ordinary tooling errors and do not infer a platform-security cause from them; only an explicit production-mutation/policy rejection establishes a security/authorization block.
- A deterministic owned tooling/coordination trap observed during current work is not an unrelated broken path when it directly caused a rejected, failed, duplicated, or partially-applied operation and has reusable recurrence value. For cheap reversible low-blast-radius fixes, repair the owning helper/wrapper/validator/guidance and add the narrowest executable recurrence check in the same run without asking the user. If exact collision or an explicit shared-production/control-plane/scheduler/security gate prevents that repair, leave one bounded owner issue/handoff with the exact failing input/shape and successful recovery, then continue in-scope work. Do not leave a known recurring trap as a prose-only worker note.
- Swarm machine routing is one shared cohort decision, not a per-worker preference. For every substantive multi-machine-capable task, use `python C:\Users\Lauri\Desktop\vault\tools\swarm_route.py route --work-id <stable-task-id> --kind <work-kind>` and obey/reuse its live assignment. A route receipt alone does not satisfy the execution requirement. For generic Linux-compatible `portable` or `portable-light` work assigned to OMEN, use `python C:\Users\Lauri\Desktop\vault\tools\swarm_exec.py --work-id <stable-task-id> --kind <portable|portable-light> --repo-root <repo> -- <command>` unless the owning repo already provides a more-specific OMEN wrapper; do not run that assigned command locally on Windows merely because the control session is on Windows. LowVRAM and genuinely Windows-only work stay on the main Windows PC. All other work is OMEN-first; use Windows only when the cohort reports OMEN unavailable/saturated for that class. The VPS is cohort-approved only for portable-light overflow plus its existing edge/coordination duties. Workers must not independently reroute around a valid cohort assignment or duplicate the same work on multiple machines.
- **RESOURCE PRESSURE PROBES ARE TELEMETRY, NOT WORKER ADMISSION AUTHORITY.** Bootstrap, fleet, routing, disk, RAM, process-count, and other machine-wide observations may describe current state, but they must never create, inherit, or justify a task/issue/worker/swarm `BLOCKED` state from a generic threshold. A disk-growing or otherwise resource-constrained operation asks its **exact repo/executor owner**, which must make the decision from a fresh direct measurement plus that operation's tested measured/predicted growth or reserve. Only that exact invocation may refuse, and a probe failure or refusal blocks only that invocation; the worker must continue another ready non-conflicting canonical contribution. Cleanup is **ownership/lifecycle driven, not pressure triggered**: worker/job closeout may asynchronously reclaim only that finished owner's positively proven inactive Git-ignored/regeneratable state, and cleanup failure must not prevent closeout. An exact executor may run one bounded owner-safe reclaim only when its own admission would otherwise fail, then re-probe once. Never use a generic machine free-space/RAM/process watermark as preflight authority, never spin reclamation toward a target number, and never substitute age/size/name/full-drive heuristics or deletion of source/Content/assets/Saved/proof/evidence/user/foreign/dirty state. OMEN-first routing remains a separate cohort decision, not a resource-pressure verdict.
- P3/UE routing: the Linux OMEN is the default build, runtime, test, and proof machine. Keep a supervised hot P3 runtime available there for immediate proof use; C++ or source changes should refresh the OMEN build/runtime path and return it to the hot state rather than defaulting to Windows. The existing OMEN repo-owned build/runtime wrappers remain the execution owners. Windows Unreal/editor/runtime is fallback or explicit Windows cross-platform acceptance only when the routing cohort reports OMEN saturated/unavailable or the exact task is Windows-specific. The OMEN GTX 1070 remains usable through the Canonical-signed NVIDIA 535.230.02 module under Secure Boot.
- **User corrections are answer-first; literal `slopwall` additionally requires a bounded durable learning loop.** A literal `slopwall` opens a pending fail-closed pre-final state. Before any normal final reply, require both: (a) concrete resumed work/result that repairs the inherited objective, and (b) an accepted `memory_bank.py record --kind correction` result for that Slopwall incident. If either is missing, do not return control with acknowledgement, status, diagnosis, or promises; continue the corrected work/durability loop. For ordinary corrections, immediately reduce the corrected request to the exact proposition/action, apply the `RULES.md` exact-fact proof gate, and give or execute the corrected result before retrospective explanation or optional durability work. Do not perform Vault writes, issue updates, incident analysis, or similar coordination merely as a prerequisite to answering an ordinary correction. **Literal `slopwall` is the durability exception, not a task-priority exception:** first resume the inherited task with concrete missing substance, then before returning control inspect the failed reply/action against the exact request; name the concrete rejected behavior; infer the best-supported mechanism or decision failure; derive one reusable prevention lesson that names the triggering condition and required different action; and persist one compact `memory_bank.py record --kind correction` entry containing the user's verbatim `slopwall`, rejected behavior, diagnosis/mechanism, and prevention lesson. The Slopwall record is mandatory and must be specific enough to change future selection behavior; do not store merely `be concise`, `answer better`, an apology, or the complaint restated. If mechanism evidence is incomplete, record the observable failure plus bounded uncertainty instead of fabricating a root cause. Do not turn the learning loop into a report/PR/policy campaign; after durability is secured, continue or finish the inherited task rather than making the learning process the user-visible product. The literal signal `asshole` remains a lightweight correction marker and does not by itself require durable logging.
- Keep work scoped to actual ownership/collision boundaries. Dirty or foreign state blocks only the exact conflicting mutation, not the whole issue.
- In manual/on-demand chats, follow the `go`/`continue` completion and yield semantics in `RULES.md`; do not substitute a plan, status update, open PR, or decision-ready checkpoint for the next routine safe step of the current coherent contribution. Immediately before final-answering a manual `go`/`continue` turn, apply the `RULES.md` pre-final yield audit; if no permitted stop condition is supported by live evidence, continue execution. Never treat elapsed wall time, tool-call count, work already completed, handoff/PR state, or an exact-scope collision as stop evidence. Landing the current contribution is not a permitted stop unless live evidence proves the user's established goal complete; never answer `go` by asking the user for a new task merely because the current contribution finished. Do not require exhaustion of unrelated execution surfaces or independent useful work. Timed workers retain their separate utilization contract. A user correction that a manual `go` was answered with apology, explanation, or status instead of execution resumes that same continuation immediately: take a concrete useful action before retrospective commentary, and never count apology/status/incident narration as progress or as satisfaction of the yield audit.
- Before yielding tracked multi-agent repo work, leave existing shared work traceable through its normal commit/branch/PR/issue path or record the exact remaining dirty paths and blocker there when needed for handoff. Do not create or update GitHub solely to end a bounded manual request, and never treat report archival/checkpointing as a reason to delay a prompt-local answer.

## Convergence and proof
- Verified useful work should converge promptly through the repo's normal branch/PR/integration path. Do not leave finished branches or duplicated implementation paths as shadow backlogs.
- **No passive integration queue.** Before opening avoidable adjacent WIP in the same repo/owner/acceptance scenario, reconcile existing non-draft PRs that already implement required work. Merge immediately through the repo's supported path when repo-required proof is satisfied. If a required check, review, or proof is pending, preserve the exact terminal gate and advance other useful work without polling; if base drift makes the PR stale or conflicting, repair/rebase that still-valid PR before creating overlapping follow-on changes. A user-marked important or high-impact regression remains load-bearing until merged or blocked by a concrete required gate; an open PR alone is not a handoff, completion state, or reason to start a parallel replacement. For repos whose terminal evidence is local/repo-owned proof, publish the exact current PR head with `Set-PullRequestConvergenceReady.ps1`; `ReadyPullRequestConvergence` then owns automatic exact-head landing/escalation. Do not publish readiness before the required proof is actually satisfied.
- For `organicoverlords/p3`, merge pull requests to `main` only through `scripts/ci/control/Invoke-P3PrMergeGuard.ps1 -Repository organicoverlords/p3 -PullRequest <number> -Execute`; do not run `gh pr merge`, enable auto-merge, or otherwise merge the PR directly. The guard's exact-head/current-main cohort decision is the merge authority. If GitHub branch protection/rulesets are unavailable under the repository's plan, absence of a server-side required check does not make this advisory.
- A build/commit/PR is supporting evidence, not the product result. Player/user-visible claims require the repo's existing supported proof/delivery route when such proof is actually needed.
- If a trap in an owned path is discovered, follow the durable-closure rule in `RULES.md`; do not leave a one-off workaround for the next agent.
